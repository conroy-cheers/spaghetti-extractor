[build-system]
requires = [ "setuptools>=69" ]
build-backend = "setuptools.build_meta"

[project]
name = "spaghetti-extractor"
version = "0.1.0"
description = "Static Windows binary reconstruction and progressive source-lifting toolkit"
requires-python = ">=3.11"
dependencies = [
  "capstone",
  "pefile",
  "z3-solver",
]

[project.optional-dependencies]
conformance = [
  "unicorn",
]

[project.scripts]
spaghetti-extractor = "spaghetti_extractor.cli:main"
spaghetti-extractor-test = "spaghetti_extractor.testkit.runner:main"
spaghetti-extractor-dev = "spaghetti_extractor.testkit.cli:main"

[project.urls]
Repository = "https://github.com/conroy-cheers/spaghetti-extractor"

[tool.setuptools.packages.find]
where = [ "src" ]

[tool.setuptools.package-data]
spaghetti_extractor = [ "lean/**/*.lean", "resources/ghidra/*.java" ]

[tool.setuptools.data-files]
"share/spaghetti-extractor/nix" = [
  "nix/bochs-conformance.nix",
  "nix/artifact-phase-v3.nix",
  "nix/artifact-set-v3.nix",
  "nix/artifact-seed-v3.nix",
  "nix/authority-workflow.nix",
  "nix/authority-diagnostics.nix",
  "nix/authority-input-exception-evidence.nix",
  "nix/authority-input-external-site-evidence.nix",
  "nix/authority-input-external-inputs.nix",
  "nix/authority-final-gate.nix",
  "nix/authority-graph-manifest.nix",
  "nix/authority-input-implementation-capabilities.nix",
  "nix/authority-isa-frontiers.nix",
  "nix/authority-input-indexed-target-evidence.nix",
  "nix/authority-input-isa-evidence.nix",
  "nix/authority-machine-ir-input.nix",
  "nix/authority-source-plan.nix",
  "nix/authority-input-standard-evidence.nix",
  "nix/authority-graph-v3-boundaries.nix",
  "nix/authority-graph-v3-packs.nix",
  "nix/authority-graph-v3.nix",
  "nix/authority-resource-classes-v3.nix",
  "nix/ca-python-json-phase.nix",
  "nix/callable-external-runtime-contract.py",
  "nix/machine-import-control-profile.nix",
  "nix/python-module-closure.nix",
  "nix/python-module-index.json",
  "nix/target-sdk.nix",
  "nix/toolkit-context.nix",
  "nix/stage-a-builder-public-keys",
  "nix/stage-a-builders",
  "nix/stage-a-external-interface-profile.nix",
  "nix/stage-a-isa-conformance.nix",
  "nix/stage-a-isa-conformance-kernel.nix",
  "nix/stage-a-isa-qualification-graph.nix",
  "nix/stage-a-isa-semantic-kernel.nix",
  "nix/stage-a-inductive-certificate-kernel.nix",
  "nix/stage-a-machine-ir-isa-qualification-v2.nix",
  "nix/stage-a-lightweight-ca-builders",
  "nix/stage-a-roundtrip-corpus.nix",
  "nix/stage-b-component-analysis.nix",
  "nix/stage-b-component-discovery.nix",
  "nix/stage-b-component-runtime-package.nix",
  "nix/stage-b-components.nix",
  "nix/stage-b-fallback-coverage-receipt.nix",
  "nix/stage-b-interpreter-package.nix",
  "nix/stage-b-linked-libraries.nix",
  "nix/stage-b-native-object-graph.nix",
  "nix/stage-b-functional-suite.nix",
  "nix/stage-b-headless-diagnostic-run.nix",
  "nix/stage-b-hybrid-candidate.nix",
  "nix/stage-b-upstream-shell-suite.nix",
]
"share/spaghetti-extractor/profiles" = [ "profiles/*.json" ]
